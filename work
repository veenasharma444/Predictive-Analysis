
Gender Analysis - Demographic Attrition Model

Cell 1

Check unique gender values

df[“EMP_GENDER”].value_counts(dropna=False)

Cell 2

Clean Gender column

df[“EMP_GENDER”] = ( df[“EMP_GENDER”] .astype(str) .str.strip()
.str.title() )

df[“EMP_GENDER”].value_counts(dropna=False)

Cell 3

Employee Distribution by Gender

gender_dist = ( df[“EMP_GENDER”] .value_counts() .reset_index() )

gender_dist.columns = [“Gender”, “Employees”] gender_dist

Cell 4

import matplotlib.pyplot as plt

plt.figure(figsize=(6,5)) plt.bar(gender_dist[“Gender”],
gender_dist[“Employees”]) plt.title(“Employee Distribution by Gender”)
plt.xlabel(“Gender”) plt.ylabel(“Number of Employees”) plt.show()

Cell 5

Gender-wise Attrition Table

gender_attrition = pd.crosstab( df[“EMP_GENDER”], df[“ATTRITION”] )

gender_attrition.columns = [“Active”, “Left”] gender_attrition

Cell 6

Calculate Attrition Percentage

gender_attrition[“Total”] = ( gender_attrition[“Active”] +
gender_attrition[“Left”] )

gender_attrition[“Attrition %”] = ( gender_attrition[“Left”] /
gender_attrition[“Total”] * 100 ).round(2)

gender_attrition

Cell 7

plt.figure(figsize=(6,5)) plt.bar(gender_attrition.index,
gender_attrition[“Attrition %”]) plt.title(“Attrition % by Gender”)
plt.xlabel(“Gender”) plt.ylabel(“Attrition %”) plt.show()

Cell 8

print(“=”50) print(“Gender Analysis Summary”) print(“=”50)
print(gender_attrition) print(“=”*50)

Cell 9

gender_attrition.reset_index().to_csv( “gender_attrition.csv”,
index=False )

print(“gender_attrition.csv saved successfully.”)
