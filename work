
Marital Status Analysis - Demographic Attrition Model

Cell 1 - Check unique values

    print("Marital Status Analysis - Demographic Attrition Model")
    df["MARITAL_STATUS"].value_counts(dropna=False)

Cell 2 - Clean Marital Status column

    df["MARITAL_STATUS"]=(df["MARITAL_STATUS"].astype(str).str.strip().str.title())
    df["MARITAL_STATUS"].value_counts(dropna=False)

Cell 3 - Employee Distribution

    marital_dist=df["MARITAL_STATUS"].value_counts().reset_index()
    marital_dist.columns=["Marital Status","Employees"]
    marital_dist

Cell 4 - Plot Distribution

    import matplotlib.pyplot as plt
    plt.figure(figsize=(8,5))
    plt.bar(marital_dist["Marital Status"], marital_dist["Employees"])
    plt.title("Employee Distribution by Marital Status")
    plt.xlabel("Marital Status")
    plt.ylabel("Employees")
    plt.show()

Cell 5 - Attrition Table

    marital_attrition=pd.crosstab(df["MARITAL_STATUS"],df["ATTRITION"])
    marital_attrition.columns=["Active","Left"]
    marital_attrition

Cell 6 - Attrition %

    marital_attrition["Total"]=marital_attrition["Active"]+marital_attrition["Left"]
    marital_attrition["Attrition %"]=(marital_attrition["Left"]/marital_attrition["Total"]*100).round(2)
    marital_attrition

Cell 7 - Plot Attrition %

    plt.figure(figsize=(8,5))
    plt.bar(marital_attrition.index, marital_attrition["Attrition %"])
    plt.title("Attrition % by Marital Status")
    plt.xlabel("Marital Status")
    plt.ylabel("Attrition %")
    plt.show()

Cell 8 - Summary

    print("="*60)
    print("Marital Status Analysis Summary")
    print("="*60)
    print(marital_attrition)
    print("="*60)

Cell 9 - Export CSV

    marital_attrition.reset_index().to_csv("marital_attrition.csv",index=False)
    print("marital_attrition.csv saved successfully.")
